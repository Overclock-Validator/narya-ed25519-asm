# Zen 5 fixed-base-comb checkpoint — 2026-07-27

This record covers standalone assembly commit
`4f508579971b61b13a4860e348c3cc597886c4e9`.

## Scope

- exact balanced radix-256 recoding of canonical scalars;
- reproducible 491,520-byte immutable affine-Niels basepoint table;
- boundary-safe masked-load x8 affine-Niels transpose assembly;
- two-way comb evaluation with 32 positions and eight doublings;
- 256 edge/random scalar fixtures generated by independent affine arithmetic;
- all 256 active masks and projective output comparison;
- complete strict-verifier regression after replacing temporary variable-base
  `[S]B` evaluation;
- warning-as-error compilation with conversion/shadow diagnostics and
  ASan/UBSan execution.

## Verdict

All listed gates passed on the Ryzen 7 9700X. This establishes correctness
evidence for the fixed-base substitution and complete verifier composition;
it is not an independent security audit or a performance claim.
